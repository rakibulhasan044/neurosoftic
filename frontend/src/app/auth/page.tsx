import { AuthPageContent } from "@/components/modules/auth/AuthPageContent";

export const metadata = {
  title: "Authentication | Neurosoftic",
  description: "Login or create an account to start shopping.",
};

export default function AuthPage() {
  return <AuthPageContent />;
}
